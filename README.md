## Обзор

Проект построен на основе **модульной архитектуры** с использованием Next.js 14+ (App Router), React 18+, TypeScript и TailwindCSS. Основной принцип организации кода — **feature-based модули** с иерархией parent → child.

---

## Структура директорий

```
src/
├── api/                    # API слой
├── app/                    # Next.js App Router (роутинг)
├── lib/                    # Глобальный layout (провайдеры, конфигурация, middleware)
├── modules/                # Функциональные модули
└── shared/                 # Переиспользуемые UI компоненты, хуки
    ├── components/         # Структурные компоненты
    ├── ui/                 # Базовые примитивы
    ├── hooks/              # Хуки
    └── lib/                # Типы, константы, утилиты для UI
```

---

## 1. API Слой (`/src/api`)

**Назначение**: Централизованный слой для взаимодействия с backend API.

### Структура
```
api/
├── module/
│   └── index.ts
│   └── queries.ts #Если юзаете Tanstack Query 
├── config.ts # общяя конфигурация fetch или axios client
├── error.ts # обработка ошибок
└── token.ts # логина рефреша токена
```

### Принципы
- Каждый модуль API представлен классом (например, `ServiceAPI`, `AuthAPI`)
- Методы класса инкапсулируют HTTP-запросы
- Типизация всех запросов и ответов через TypeScript
- Централизованная обработка ошибок через `error.ts`

**Пример**: `src/api/services/index.ts` экспортирует класс `ServiceAPI` с методами:
- `getData()` — получение данных сервиса
- `createOrder()` — создание заказа
- `upgrade()` — обновление конфигурации

---

## 2. Модули (`/src/modules`)

**Назначение**: Функциональные модули приложения, каждый отвечает за определенную бизнес-логику.

### Структура модуля
```
modules/
├── auth/                   # Родительский модуль
│   ├── login/             # Дочерний модуль (страница)
│   │   ├── components/    # Компоненты страницы
│   │   │   └── login-form.tsx
│   │   ├── lib/           # Локальные утилиты модуля
│   │   │   ├── types.ts
│   │   │   └── use-login.ts
│   │   └── index.tsx      # ❗ Страница LoginPage
│   ├── register/
│   └── shared/            # Общие компоненты для модуля auth
└── services/
```

### Ключевые принципы

#### 🔹 Parent → Child иерархия
Модули организованы иерархически:
- **Родительский модуль** (например, `auth`, `services/vds`) группирует связанные функции
- **Дочерние модули** (например, `login`, `register`, `detail/config`) представляют конкретные страницы или подфункции

#### 🔹 index.tsx vs index.ts

| Файл | Назначение | Пример |
|------|------------|--------|
| **index.tsx** | **Страница** (page-level компонент) | `LoginPage`, `VdsConfigPage` |
| **index.ts** | **Barrel export** (переэкспорт субмодулей) | `export * from "./login-form"` |

**Почему так?**
- `index.tsx` — это точка входа для страницы, которая рендерится Next.js роутером
- `index.ts` в подпапках (`components/`, `hooks/`, `lib/`) используется для удобного импорта:
  ```typescript
  // Вместо:
  import { ConfigChangeDialog } from "./components/config-change-dialog";
  import { TemplateChangeDialog } from "./components/template-change-dialog";
  
  // Используем:
  import { ConfigChangeDialog, TemplateChangeDialog } from "./components";
  ```

#### 🔹 Структура дочернего модуля (страницы)
```
module-name/
├── components/          # UI компоненты страницы
│   ├── component-a.tsx
│   ├── component-b.tsx
│   └── index.ts        # ⚠️ Barrel export (опционально, см. ниже)
├── lib/                # Локальные утилиты, типы, константы, хуки
│   ├── types.ts
│   ├── use-data.ts
│   ├── constants.ts
│   ├── helpers.ts
│   └── index.ts        # ⚠️ Barrel export (опционально)
└── index.tsx           # Главный компонент страницы (Page)
```

**Важно**: 
- `lib/` в модуле содержит **локальные** типы/константы/утилиты/хуки, специфичные для данного модуля
- Если используется глобально в нескольких модулях — перенеси в `/shared/`

⚠️ **О Barrel Exports (`index.ts`):**
- **Используй** когда страница импортирует 2+ компонента из одной папки
- **Не используй** если импортируешь только 1 компонент (не нужное доп слой)
- **Альтернатива**: прямые импорты `import { Form } from "./components/form"`

---

## 3. Shared (`/src/shared`)

**Назначение**: Переиспользуемые компоненты, хуки и утилиты, используемые в нескольких модулях.

### Структура
```
shared/
├── components/          # Структурные/композитные компоненты
│   ├── action-card/     # Карточка с действиями
│   ├── nav-link.tsx     # Навигационная ссылка
│   ├── form/            # Обёртки для форм
│   ├── table/           # Композитная таблица
│   ├── input/           # Mask-input и т.д.
│   └── ...
├── hooks/               # Переиспользуемые хуки
├── store/               # Глобальное состояние (Zustand/Redux)
├── ui/                  # Базовые UI примитивы (shadcn/ui)
│   ├── button.tsx       # Базовая кнопка
│   ├── input.tsx        # Базовый инпут
│   ├── checkbox.tsx
│   ├── dialog.tsx
│   └── ...
├── lib/                 # Переиспользуемые типы, константы, утилиты
│   ├── types.ts         # Типы, используемые в нескольких модулях
│   └── constants.ts     # Константы
└── utils/               # Утилиты (форматирование и т.д.)
```

### Принципы

#### 🔹 Разница между `ui/` и `components/`

| Категория | Назначение | Примеры |
|-----------|------------|---------|
| **`ui/`** | **Базовые UI примитивы** — низкоуровневые компоненты без бизнес-логики | `Button`, `Input`, `Checkbox`, `Dialog`, `Select` |
| **`components/`** | **Структурные компоненты** — композиция примитивов с дополнительной логикой | `NavLink`, `ActionCard`, `Table`, `Form` |

**Пример**:
- `shared/ui/button.tsx` — просто кнопка с вариантами стилей
- `shared/ui/input.tsx` — базовый инпут
- `shared/components/nav-link.tsx` — композиция `Link` + `Icon` + `Text` с логикой активного состояния
- `shared/components/input/mask-input.tsx` — инпут с маской (телефон, карта)

#### 🔹 Что такое `shared/lib/`?

`shared/lib/` — переиспользуемые типы, константы.

**Различие с `/src/lib/`**:
- `/src/lib/` — глобальная конфигурация приложения (провайдеры, middleware)
- `/src/shared/lib/` — переиспользуемые типы и утилиты для компонентов

**Пример**:
```typescript
// ✅ /src/lib/query-client.tsx
export const queryClient = new QueryClient({ ... }); // Конфигурация

// ✅ /src/shared/lib/types.ts  
export type Status = "active" | "inactive"; // Типы для UI

// ✅ /src/shared/utils/format-date.ts
export const formatDate = (date: Date) => ...; // UI утилита
```

#### 🔹 Общие правила
- Все компоненты должны быть **агностичными** к бизнес-логике модулей
- Хорошие примеры: `NavLink`, `ActionCard`, `Table`, `SearchInput`
- Плохие примеры: `VdsConfigCard`, `BillingInvoiceTable` (специфичны для модулей)

---

## 4. Lib (`/src/lib`)

**Назначение**: Глобальный layout приложения — провайдеры, конфигурация, middleware.

### Содержимое
```
lib/
├── amplitude-provider.tsx      # Провайдер аналитики
├── tanstack-provider.tsx       # Провайдер TanStack Query
├── query-client.tsx            # Конфигурация React Query
├── confirm-dialog-provider.tsx # Глобальный диалог подтверждения
├── middleware-helpers.ts       # Хелперы для Next.js middleware
└── verify-guard.tsx            # Auth guard
```

### Принципы
- **Глобальный уровень приложения**: провайдеры, которые оборачивают все приложение
- **Конфигурация**: настройка библиотек (Tanstack Query, etc.)
- **Middleware**: Next.js middleware и guards
- **Не UI компоненты**: здесь только инфраструктура, а не переиспользуемые UI элементы

### Что НЕ должно быть в `lib/`
❌ UI компоненты (кнопки, инпуты, карточки)  
❌ Хуки для UI логики  
❌ Типы для компонентов  

**Ключевое различие: `lib/` vs `shared/`**

| Директория | Назначение | Примеры |
|------------|------------|---------|
| **`lib/`** | **Глобальный layout** — провайдеры, конфигурация, middleware | `TanstackProvider`, `AmplitudeProvider`, middleware helpers |
| **`shared/`** | **Переиспользуемые UI** — компоненты, хуки, утилиты | UI компоненты, хуки, типы, utils |

**Простое правило**:
- `lib/` = То, что оборачивает приложение в `layout.tsx` или используется на уровне конфигурации
- `shared/` = То, что используется внутри страниц и компонентов

**Пример из `layout.tsx`**:
```typescript
import { AmplitudeProvider } from "@/lib/amplitude-provider"; // ✅ lib - провайдер
import { TanstackProviders } from "@/lib/tanstack-provider";   // ✅ lib - провайдер
import { Toaster } from "@shared/ui/sonner";                  // ✅ shared/ui - компонент

export default function RootLayout({ children }) {
  return (
    <TanstackProviders>           {/* Провайдер из lib/ */}
      <AmplitudeProvider>         {/* Провайдер из lib/ */}
        {children}
        <Toaster />               {/* UI компонент из shared/ */}
      </AmplitudeProvider>
    </TanstackProviders>
  );
}
```

---

## Правила именования и экспорта

### `index.tsx` vs `index.ts`

| Файл | Назначение | Когда использовать |
|------|------------|-------------------|
| **`index.tsx`** | **Страница модуля** | Главный компонент страницы (например, `LoginPage`) |
| **`index.ts`** | **Barrel export** | Когда импортируешь 2+ компонента из одной папки вместе |
| **`name-block.tsx`** | **Блок/компонент** | Обычный компонент, не страница (например, `login-form.tsx`) |

### О Barrel Exports

**Barrel export** (`index.ts`) — файл, который реэкспортирует несколько модулей:

```typescript
// components/index.ts
export * from "./login-form";
export * from "./social-buttons";

// Использование
import { LoginForm, SocialButtons } from "./components"; // Оба компонента
```

**Правило**:
- ✅ Используй для 2+ компонентов, которые импортируются вместе
- ⚠️ Не используй для 1 компонента (лишний слой, лучше прямой импорт)

```typescript
// ⚠️ Неоптимально
import { LoginForm } from "./components"; // через index.ts

// ✅ Оптимально  
import { LoginForm } from "./components/login-form"; // напрямую
```

---

## Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────────┐
│                         User Request                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                   Next.js App Router                         │
│                   (/app directory)                           │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                Module Page Component                         │
│           (modules/*/index.tsx - LoginPage)                  │
└──────────────────────────┬──────────────────────────────────┘
                           │
                ┌──────────┴──────────┐
                │                     │
                ▼                     ▼
┌──────────────────────┐   ┌──────────────────────┐
│  Module Components   │   │     API Layer        │
│  (components/)       │   │   (api/*/index.ts)   │
└──────────┬───────────┘   └──────────┬───────────┘
           │                          │
           │                          ▼
           │               ┌─────────────────────┐
           │               │   TanStack Query    │
           │               │   (lib/query-*)     │
           │               └──────────┬──────────┘
           │                          │
           │                          ▼
           │               ┌─────────────────────┐
           │               │   Backend API       │
           │               └─────────────────────┘
           │
           ▼
┌──────────────────────┐
│  Shared Components   │
│   (shared/*)         │
└──────────────────────┘
```

---

## Примеры использования

### Пример 1: Создание новой страницы логина

```
modules/auth/login/
├── components/
│   ├── login-form.tsx          # Форма логина
│   ├── social-buttons.tsx      # Кнопки соц. сетей
│   ├── remember-me.tsx         # Чекбокс "Запомнить меня"
│   └── index.ts                # ✅ Barrel export (используем 3 компонента)
├── lib/
│   ├── types.ts                # type LoginFormData = { ... }
│   └── use-login.ts            # Custom hook для логина
└── index.tsx                   # LoginPage компонент
```

**index.tsx** (страница):
```typescript
import { AuthLayout } from "@auth/shared/components/auth-layout";
import { LoginForm, SocialButtons, RememberMe } from "./components";

export const LoginPage = () => {
  return (
    <AuthLayout title="Вход">
      <LoginForm />
      <RememberMe />
      <SocialButtons />
    </AuthLayout>
  );
};
```

**components/index.ts** (barrel export):
```typescript
// Barrel export оправдан: несколько компонентов используются вместе
export * from "./login-form";
export * from "./social-buttons";
export * from "./remember-me";
```

### Пример 2: API запрос из страницы

```typescript
// modules/services/vds/detail/config/index.tsx
import { useServiceQuery } from "@/api/services/queries";
import { ConfigChangeDialog } from "./components";

export const VdsConfigPage = ({ serverId }: Props) => {
  // Используем TanStack Query + API слой
  const { data, isPending } = useServiceQuery({ serviceId: serverId });

  if (isPending) return <Skeleton />;

  return (
    <>
      <ActionCard title="Конфигурация">
        <ConfigChangeDialog serviceId={serverId} />
      </ActionCard>
    </>
  );
};
```

### Пример 3: Shared компонент (структурный)

```typescript
// shared/components/nav-link.tsx
"use client";

import { useActivePath } from "@shared/hooks/use-active-path";
import { cn } from "@/lib/utils";
import Link from "next/link";
import type { LucideIcon } from "lucide-react";

interface NavLinkProps {
  name: string;
  link: string;
  icon?: LucideIcon;
  className?: string;
}

export const NavLink = ({ link, name, icon: Icon, className }: NavLinkProps) => {
  const isActive = useActivePath(link);

  return (
    <Link
      href={link}
      className={cn(
        "flex items-center gap-2 px-2 py-1 rounded-md",
        isActive ? "bg-primary text-white" : "hover:bg-gray-100",
        className
      )}
    >
      {Icon && <Icon className="h-4 w-4" strokeWidth={isActive ? 2 : 1} />}
      <p>{name}</p>
    </Link>
  );
};
```

**Почему это в `components/`, а не в `ui/`?**
- Композиция нескольких элементов (Link + Icon + текст)
- Содержит логику (проверка активного пути через хук)
- Использует Next.js Link и custom hook

**Пример базового UI примитива:**
```typescript
// shared/ui/button.tsx
import { cn } from "@/lib/utils";

interface ButtonProps {
  variant?: "default" | "destructive" | "outline";
  size?: "sm" | "md" | "lg";
  children: React.ReactNode;
  onClick?: () => void;
}

export const Button = ({ variant = "default", size = "md", children, onClick }: ButtonProps) => {
  return (
    <button
      onClick={onClick}
      className={cn(
        "rounded-md font-medium",
        variant === "default" && "bg-primary text-white",
        variant === "destructive" && "bg-red-500 text-white",
        size === "sm" && "px-3 py-1 text-sm"
      )}
    >
      {children}
    </button>
  );
};
```

---

## Best Practices

### ✅ DO (Делай)

1. **Организуй по фичам, а не по типам файлов**
   ```
   ✅ modules/auth/login/...
   ❌ components/auth/LoginForm.tsx
   ```

2. **Используй алиасы импортов**
   ```typescript
   ✅ import { Button } from "@shared/ui/button";
   ❌ import { Button } from "../../../shared/ui/button";
   ```

3. **Держи типы и константы рядом с кодом**
   ```
   ✅ modules/auth/login/lib/types.ts       (локальные типы модуля)
   ✅ modules/auth/login/lib/constants.ts   (константы модуля)
   ✅ shared/lib/types.ts                   (переиспользуемые типы)
   ✅ shared/lib/constants.ts               (переиспользуемые константы)
   ```
   
   **Правило**: Если используется только в одном модуле → `module/lib/`, если в нескольких → `shared/lib/`

### ❌ DON'T (Не делай)

1. **Не создавай глубокие вложенности (> 4 уровней)**
   ```
   ❌ modules/services/vds/detail/config/components/forms/inputs/text-input.tsx
   ✅ modules/services/vds/detail/config/components/config-input.tsx
   ```

2. **Не дублируй компоненты**
   - Если компонент используется в 2+ модулях → перенеси в `shared/`

3. **Не смешивай бизнес-логику в shared/**
   ```
   ❌ shared/components/vds-config-card.tsx     (специфично для VDS)
   ❌ shared/components/billing-invoice-row.tsx (специфично для Billing)
   ✅ shared/components/action-card.tsx         (универсальный)
   ✅ shared/components/nav-link.tsx            (универсальный)
   ✅ shared/ui/button.tsx                      (базовый примитив)
   ```
   
   **Правило**: Если компонент используется только в одном модуле — он не должен быть в `shared/`


---

## Миграция существующего кода

При рефакторинге старого кода:

1. **Определи уровень модуля** (auth, services, billing)
2. **Создай структуру папок** (components, lib)
3. **Обнови импорты** (используй алиасы `@shared`, `@modules`)
4. **Разнеси компоненты**: базовые → `shared/ui/`, структурные → `shared/components/`
5. **Перенеси типы и константы** в `lib/` (локальные в модуль, глобальные в `shared/lib/`)

---

## Заключение

Эта архитектура обеспечивает:
- ✅ **Масштабируемость** — легко добавлять новые модули
- ✅ **Поддерживаемость** — четкое разделение ответственности
- ✅ **Читаемость** — понятная структура и именование
- ✅ **DRY принцип** — переиспользование через `shared/`
- ✅ **Type Safety** — полная типизация через TypeScript
- ✅ **Модульность** — parent → child иерархия модулей

### Ключевые принципы

- **Модули** (`modules/`) — функциональные фичи приложения
- **Shared UI** (`shared/ui/`) — базовые примитивы (button, input)
- **Shared Components** (`shared/components/`) — структурные компоненты (nav-link, table)
- **Shared Lib** (`shared/lib/`) — переиспользуемые типы, константы, утилиты для UI
- **Lib** (`lib/`) — глобальный layout (провайдеры, конфигурация, middleware)
- **API** (`api/`) — централизованный слой для backend

При возникновении вопросов или необходимости рефакторинга — обращайся к этому документу.

